{
  "name": "rollup-ui-storeroom",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bostrap": "yarn",
    "build:ui": "yarn workspace ui build",
    "build:theme": "yarn workspace ui build:theme",
    "dev:website": "yarn workspace website dev"
  }
}